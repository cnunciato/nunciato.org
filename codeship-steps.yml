- service: web
  build:
    image: cnunciato/nunciato-org
    dockerfile_path: ./Dockerfile
  command: npm test

- type: serial
  steps:
    - name: dockerhub_push
      tag: master
      service: web
      type: push
      image_name: cnunciato/nunciato-org
      # image_tag: latest
      registry: https://index.docker.io/v1/
      encrypted_dockercfg_path: dockercfg.encrypted
