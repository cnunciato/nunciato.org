{
  "name": "chris.nunciato.org",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Warning: no test specified\" && exit 0",
    "start": "http-server -p 4000 -o -c-1",
    "surge": "surge --project ./"
  },
  "devDependencies": {
    "http-server": "^0.9.0",
    "surge": "^0.18.0"
  }
}
