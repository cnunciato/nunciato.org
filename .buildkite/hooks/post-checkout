#!/usr/bin/env bash

set -euo pipefail

# echo "Installing Mise..."
curl https://mise.run | sh

# # echo "Installing Mise-managed tooling..."
# $(pwd)/tools/mise generate bootstrap

# export PATH="$(mise where node)/bin:$PATH"
which node
node -v
