#!/usr/bin/env bash

set -euo pipefail

# echo "Installing Mise..."
curl https://mise.run | sh
echo "eval \"\$(/Users/agent/.local/bin/mise activate zsh)\"" >> "/Users/agent/.zshrc"

which node
node -v
