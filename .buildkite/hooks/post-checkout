#!/usr/bin/env bash

set -euo pipefail

# echo "Installing Mise..."
curl https://mise.run | MISE_INSTALL_PATH="$(pwd)/tools/mise" sh

# echo "Installing Mise-managed tooling..."
mise install

export PATH="$(mise where node)/bin:$PATH"
which node
node -v
