{
    "name": "chris.infra",
    "main": "dist/index.js",
    "devDependencies": {
        "@pulumi/pulumi": "^3",
        "@repo/pulumi-astro-aws": "*",
        "typescript": "^5.5.4"
    },
    "scripts": {
        "build": "tsc",
        "preview:dev": "npm run build && pulumi stack select dev && pulumi preview",
        "deploy:dev": "npm run build && pulumi stack select dev && pulumi up --yes",
        "destroy:dev": "pulumi stack select dev && pulumi destroy --yes"
    }
}
